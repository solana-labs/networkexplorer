import {makeStyles} from '@material-ui/core';

export default makeStyles({
  root: {
    position: 'relative',
    marginBottom: 47,
  },
  title: {
    display: 'flex',
    alignItems: 'center',
  },
  decor: {
    position: 'absolute',
    left: -120,
    top: -7,
  },
});
