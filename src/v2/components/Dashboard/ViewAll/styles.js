import {makeStyles} from '@material-ui/core';

export default makeStyles({
  root: {
    position: 'relative',
  },
  title: {
    marginBottom: 70,
  },
  decor: {
    position: 'absolute',
    left: -120,
    top: 32,
  },
});
