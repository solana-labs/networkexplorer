import {makeStyles} from '@material-ui/core';

export default makeStyles({
  root: {
    padding: '40px 0',
    position: 'relative',
  },
  title: {
    marginBottom: 70,
  },
  decor: {
    position: 'absolute',
    left: -120,
    top: 32,
  },
});
